{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS='--openssl-legacy-provider' vite",
    "build": "NODE_OPTIONS='--openssl-legacy-provider' vite build",
    "preview": "NODE_OPTIONS='--openssl-legacy-provider' vite preview",
    "css:build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "css:watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.15",
    "vite": "^7.0.2"
  }
}
