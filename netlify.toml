[build]
  base = "frontend"
  command = "bash ../netlify-plugin-clean.sh && npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_ENV = "production"
  NODE_OPTIONS = "--openssl-legacy-provider --max_old_space_size=4096"
  VITE_API_URL = "your-api-url"
